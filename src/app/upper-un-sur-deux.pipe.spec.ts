import { UpperUnSurDeuxPipe } from './upper-un-sur-deux.pipe';

describe('UpperUnSurDeuxPipe', () => {
  it('create an instance', () => {
    const pipe = new UpperUnSurDeuxPipe();
    expect(pipe).toBeTruthy();
  });
});
